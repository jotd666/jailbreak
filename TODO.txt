- sometimes character changes places... highly buggy...
  we have to find which part is changing the character position that much
  or change the scroll that much???
- DSW for lives number is wrong
- explosion for molotov wrong sprite (bullet instead of explosion)
- highscore entry: missing joystick read? missing time elapsing  
- sprites should be displayed even if more on the left
  (disappear too fast)
- double buffering to implement
- optimize: remove some sprites from mirror to save memory
  (vehicles)
- create an excluding config to remove some parasite sprite combinations 
- rip voices!

log sounds: bpset xxx,,{print A;g}
sim sound on credit: bpset xxx,,{A=$81;g}
