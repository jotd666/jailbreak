- check for hardest difficulty dip switch is in effect here!
- broken: tile bitplane not displayed anymore: put 555 AAA pattern t detect it
- remove 25Hz mode, just skip frames if cannot cope
- use sprites for player bullets / enemy bullets when possible
  with fallback to bobs: sprite 0x100
- cheat key (F4) to enable/disable invincibility, check killing the warden
  with no cheats on
- easy mode: if hit, just decrease weapon level
- vertical scrolling for end sequence: not perfect...

- sounds not playing, intro music when ends should play level description (siren, "hostage" message, hack to make them play)
  there's a lot of code in high frequency interrupt that is now not reached
- some sounds seem not used ("Broadway" as music doesn't play in level 1)
- command line for lives number is wrong: if user enters 4 ???
- double buffering but still a lot of blinking on chip only

- optimize: remove some sprites from mirror to save memory
  (vehicles)
- cheat start level (identify level number)
- use multiplexed sprites for enemy bullets (maybe)
- ECS version!

log sounds: bpset 93b7,,{print A;g}
only play this C sound: bpset 93b7,A!=$C,{PC=93c9;g}
sim sound on credit: bpset 93b7,,{A=$81;g}


    <cheat desc="Select Starting Stage">
        <comment>Both players share starting stage</comment>
        <parameter min="01" max="05" step="01" />
        <script state="run">
            <action condition="maincpu.pb@1597 == 01">
                maincpu.pb@1596 = maincpu.pb@159B = param - 1,
                maincpu.pb@1597 = param,
                maincpu.pd@15A0 = FFFFFF00 | (maincpu.pd@15A0 band ~FFFFFF00)
            </action>
        </script>
    </cheat>